# Environment Configuration for Render Deployment
# Copy these variables to Render's Environment Variables section

# Environment
NODE_ENV=production

# Server (Render uses PORT 10000 by default)
PORT=10000

# Database Configuration
# Get your connection string from MongoDB Atlas
DATABASE=mongodb+srv://<USERNAME>:<PASSWORD>@cluster0.mongodb.net/vistara?retryWrites=true&w=majority
DATABASE_PASSWORD=your_database_password_here

# JWT Configuration
# Generate a secure secret: openssl rand -base64 32
JWT_SECRET=your-ultra-secure-jwt-secret-key-min-32-characters-long
JWT_EXPIRES_IN=90d
JWT_COOKIES_EXPIRES_IN=90

# Email Configuration - Development (Mailtrap)
# Sign up at https://mailtrap.io for testing
EMAIL_HOST=sandbox.smtp.mailtrap.io
EMAIL_PORT=25
EMAIL_USERNAME=your_mailtrap_username
EMAIL_PASSWORD=your_mailtrap_password

# Email Configuration - Production (SendGrid)
# Get API key from https://sendgrid.com
EMAIL_FROM=your-verified-sender@example.com
SENDGRID_USERNAME=apikey
SENDGRID_PASSWORD=your_sendgrid_api_key_here

# Stripe Payment Integration
# Get keys from https://dashboard.stripe.com/apikeys
# Use test keys for development, live keys for production
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here

# Optional: Frontend URL (for CORS in production)
# FRONTEND_URL=https://your-app.onrender.com

# ====================================================================
# RENDER DEPLOYMENT NOTES:
# ====================================================================
# 1. Do NOT commit config.env or .env to version control
# 2. Add each variable individually in Render Dashboard → Environment
# 3. MongoDB Atlas: Whitelist IP 0.0.0.0/0 in Network Access
# 4. Stripe: Update webhook URL to https://your-app.onrender.com
# 5. SendGrid: Verify sender email before using in production
# ====================================================================
